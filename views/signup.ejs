<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>CrimeIntel | Agent Registration</title>

<style>
* { margin: 0; padding: 0; box-sizing: border-box; }

body {
  height: 100vh;
  font-family: "Inter", "Segoe UI", sans-serif;
  background:
    radial-gradient(circle at 20% 20%, rgba(255,0,0,0.12), transparent 40%),
    linear-gradient(#050608, #050608);
  color: #eaeaea;
  overflow: hidden;
}

/* GRID OVERLAY */
body::before {
  content: "";
  position: fixed;
  inset: 0;
  background:
    linear-gradient(rgba(255,255,255,0.04) 1px, transparent 1px),
    linear-gradient(90deg, rgba(255,255,255,0.04) 1px, transparent 1px);
  background-size: 40px 40px;
  pointer-events: none;
}

/* LAYOUT */
.wrapper {
  height: 100%;
  display: grid;
  grid-template-columns: 1.2fr 1fr;
}

/* LEFT PANEL */
.left {
  padding: 80px;
  display: flex;
  flex-direction: column;
  justify-content: center;
}

.brand {
  font-size: 56px;
  font-weight: 800;
  letter-spacing: 3px;
}
.brand span { color: #e53935; }

.tagline {
  max-width: 520px;
  margin-top: 20px;
  color: #b0b0b0;
  line-height: 1.6;
}

.status {
  margin-top: 30px;
  display: flex;
  align-items: center;
  gap: 10px;
  font-size: 14px;
  color: #9f9f9f;
}

.status::before {
  content: "";
  width: 10px;
  height: 10px;
  background: #e53935;
  border-radius: 50%;
  animation: pulse 1.5s infinite;
}

@keyframes pulse {
  0% { box-shadow: 0 0 0 0 rgba(229,57,53,.7); }
  70% { box-shadow: 0 0 0 12px rgba(229,57,53,0); }
  100% { box-shadow: 0 0 0 0 rgba(229,57,53,0); }
}

/* RIGHT PANEL */
.right {
  display: flex;
  align-items: center;
  justify-content: center;
}

.card {
  width: 400px;
  background: rgba(15,17,22,0.95);
  border: 1px solid rgba(255,255,255,0.08);
  padding: 40px;
  border-radius: 12px;
  box-shadow: 0 0 40px rgba(229,57,53,.15);
}

.card h2 {
  letter-spacing: 2px;
  margin-bottom: 6px;
}

.card p {
  font-size: 13px;
  color: #999;
  margin-bottom: 26px;
}

.error {
  background: rgba(229,57,53,.15);
  color: #ff6b6b;
  padding: 10px;
  border-radius: 6px;
  font-size: 13px;
  margin-bottom: 16px;
  text-align: center;
}

label {
  font-size: 12px;
  letter-spacing: 1px;
  color: #aaa;
}

input {
  width: 100%;
  margin-top: 6px;
  margin-bottom: 18px;
  padding: 12px;
  background: #080a0f;
  border: 1px solid #333;
  color: #fff;
  border-radius: 6px;
}

input:focus {
  border-color: #e53935;
  outline: none;
}

.btn {
  width: 100%;
  padding: 12px;
  background: #e53935;
  color: white;
  border: none;
  border-radius: 6px;
  font-weight: 700;
  cursor: pointer;
}

.btn:hover { background: #ff3b3b; }

.google {
  margin-top: 14px;
  background: transparent;
  border: 1px solid #444;
}

.links {
  margin-top: 20px;
  text-align: center;
  font-size: 13px;
}

.links a {
  color: #e53935;
  text-decoration: none;
}
</style>
</head>

<body>
<div class="wrapper">

  <!-- LEFT -->
  <div class="left">
    <div class="brand">CRIME<span>INTEL</span></div>
    <div class="tagline">
      Secure agent onboarding for the CrimeIntel system.
      Access is logged, monitored and verified under
      smart policing protocols.
    </div>
    <div class="status">CLEARANCE REQUIRED · VERIFICATION IN PROGRESS</div>
  </div>

  <!-- RIGHT -->
  <div class="right">
    <div class="card">
      <h2>AGENT REGISTRATION</h2>
      <p>Request secure system access</p>

      <% if (typeof error !== 'undefined' && error) { %>
        <div class="error"><%= error %></div>
      <% } %>

      <form action="/signup" method="POST">
        <label>AGENT NAME</label>
        <input type="text" name="name" required>

        <label>OFFICIAL EMAIL</label>
        <input type="email" name="email" required>

        <label>SECURITY KEY</label>
        <input type="password" name="password" minlength="6" required>

        <label>CONFIRM SECURITY KEY</label>
        <input type="password" name="confirmPassword" required>

        <button class="btn">REQUEST CLEARANCE</button>
      </form>

      <a href="/auth/google">
        <button class="btn google">REGISTER WITH GOOGLE</button>
      </a>

      <div class="links">
        Already cleared? <a href="/login">Authorize Access</a>
      </div>
    </div>
  </div>

</div>
</body>
</html>