# scikit-learn is pinned to 1.6.1 because hotspot_classifier.pkl was serialised
# with that version. Loading it under a different minor release raises
# InconsistentVersionWarning and is not guaranteed to reproduce the same
# predictions. xgboost 2.0.3 matches the booster config in the regressor.
fastapi==0.110.0
uvicorn[standard]==0.29.0
scikit-learn==1.6.1
xgboost==2.0.3
pandas==2.2.1
numpy==1.26.4
joblib==1.3.2
pydantic==2.6.4
ortools==9.9.3963
httpx==0.27.0
pytest==8.1.1
